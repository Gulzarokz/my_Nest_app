import { ExcetionFilterFilter } from './excetion-filter.filter';

describe('ExcetionFilterFilter', () => {
  it('should be defined', () => {
    expect(new ExcetionFilterFilter()).toBeDefined();
  });
});
